import { FilterServicesPipe } from './filter-services.pipe';

describe('FilterServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
